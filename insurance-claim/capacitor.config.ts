import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.insuranceclaim',
  appName: 'insurance-claim',
  webDir: 'dist/insurance-claim/browser'
};

export default config;
